import express from "express";
const router = express.Router();

// Example GET route
router.get("/add-task", (req, res) => {

});

// Example POST route
router.post("/all-projects", (req, res) => {

});

export default router;
